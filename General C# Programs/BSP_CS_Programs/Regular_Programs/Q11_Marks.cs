//Q11. Write a program to enter student marks and calculate percentage and grade.

/*using System;

namespace BSP_CS_Programs
{
    class Q11_Marks
    {
        static void Main()
        {
            int sub1, sub2, sub3;
            int sum = 0;
            float per;
            char grade = 'A';
            Console.WriteLine("Enter Your Marks in Subject 1:");
            sub1 = Convert.ToInt32(Console.ReadLine());
            Console.WriteLine("Enter Your Marks in Subject 2:");
            sub2 = Convert.ToInt32(Console.ReadLine());
            Console.WriteLine("Enter Your Marks in Subject 3:");
            sub3 = Convert.ToInt32(Console.ReadLine());

            sum = sub1 + sub2 + sub3;
            per = ((float)sum / 300) * 100;

            if(per<40.0)
            {
                grade = 'F';
            }
            else if(per>=40.0 && per<60.0)
            {
                grade = 'D';
            }
            else if(per>=60.0 && per<70.0)
            {
                grade = 'C';
            }
            else if(per>=70.0 && per<90.0)
            {
                grade = 'B';
            }
            else if(per>=90.0)
            {
                grade = 'A';
            }
            Console.WriteLine("------------------------------------");
            Console.WriteLine("Subject 1: " + sub1);
            Console.WriteLine("Subject 2: " + sub2);
            Console.WriteLine("Subject 3: " + sub3);
            Console.WriteLine("------------------------------------");

            Console.WriteLine("Total Percentage: " + per + "%");
            Console.WriteLine("Grade: " + grade);
            Console.WriteLine("------------------------------------");

        }
    }
}*/






