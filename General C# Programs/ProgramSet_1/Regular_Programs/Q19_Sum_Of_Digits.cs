//Q19. Write a program to find sum of all digits in given number.

/*using System;

namespace BSP_CS_Programs
{
    class Q19_Sum_Of_Digits
    {
        static void Main()
        {
            int num, rem, temp, sum = 0;

            Console.WriteLine("Enter a number:");
            num = Convert.ToInt32(Console.ReadLine());

            temp = num;
            while (temp != 0)
            {
                rem = temp % 10;
                sum += rem;
                temp /= 10;
            }
            Console.WriteLine("Sum of Digits of " + num + " = " + sum);
        }
    }
}*/
